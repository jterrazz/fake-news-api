app:
  env: development
  port: 3000
  host: localhost
  newRelic:
    enabled: true
  databaseUrl: file:./database/main.sqlite

logging:
  level: info

api:
  openRouter:
    budget: paid
  worldNews:
    useCache: false
