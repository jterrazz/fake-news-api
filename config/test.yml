app:
  env: test
  port: 3000
  host: localhost
  newRelic:
    enabled: false
  databaseUrl: file:./database/test.sqlite

logging:
  level: debug

api:
  worldNews:
    apiKey: test-world-news-key
  openRouter:
    apiKey: test-openrouter-key
    budget: free
